<!--%= stylesheet_link_tag "filename" %-->
<div class="container mt">

  <h1 class="myh center">Productos & Servicios</h1>

  <% if @products.blank? %>
    <h4>No hay productos</h4>
  <% else %>
    <div class="row">
      <% @products.each do |p| %>
        <div class="col-sm-3 row-eq-height">
          <div class="my-thumbnail">
            <div class="opacity">
              <%= link_to product_path(p) do %>
                <div class="superCenter img-div-size" style="">
                  <%= image_tag p.product_image.url(:medium), alt: "", style:"max-height:200px;", class:"img-rounded" %>
                </div>
                <!--img src="/w3images/lights.jpg" alt="Lights" style="width:100%"-->
                <div class="caption container-fluid caption-div-size" style="">
                  <h4><%= p.name %></h4>
                  <% if p.p_type == 1 %>
                    <h6>Producto</h6>
                  <% else %>
                    <h6>Servicio</h6>
                  <% end %>
                  <h6><%= p.category_name  %></h6>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
  <div class="offset-md-4 col-md-7">
  <ul class="pagination pagination-lg"> <%= will_paginate @products, renderer: BootstrapPagination::Rails, class: "pagination pagination-lg", :previous_label => t("Anterior"), :next_label => t("Siguiente")%> </ul>
</div>
</div>
