<%= form_with(model: product, local: true, class:"myform") do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="row">
    <div class="offset-md-4 col-md-4">
      <div class="center">
        <%= image_tag @product.product_image.url(:medium), class: "img-rounded"%>
      </div>
      <div class="form-group">
        <%= form.file_field :product_image %>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="offset-md-4 col-md-4">
      <div class="form-group">
        <%= form.label "Nombre" %>
        <%= form.text_field :name, id: :product_name, autofocus: true, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= form.label "Tipo" %>
        <%= select_tag(:p_type, options_for_select([['Producto', 1], ['Servicio', 2]])) %>
      </div>
      <div class="form-group">
        <%= form.label "Descripción" %>
        <%= form.text_field :description, id: :product_description, autofocus: true, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= form.label "Tipo" %>
        <%= select_tag(:category, options_for_select(@categories.collect{ |c| [c.name, c.id]})) %>
      </div>


      <%= form.hidden_field :available, :value=>true %>

        <!--%= link_to 'Crear Producto', show_path(@product),class: 'btn my-btn' %-->
        <%= link_to 'Cancelar', :user_home, class: 'btn' %>
        <%= form.submit t('.new_prod', default: 'Crear producto'), class: 'btn  btn-right' %>

    </div>
  </div>
<% end %>
